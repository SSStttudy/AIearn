import {describe,expect,it} from "vitest";import {decryptSecret,encryptSecret} from "./secret.js";import {randomBytes} from "node:crypto";
describe("encrypted API keys",()=>{it("round trips and randomizes ciphertext",()=>{const key=randomBytes(32),a=encryptSecret("sk-test",key,"one"),b=encryptSecret("sk-test",key,"one");expect(a.ciphertext).not.toBe(b.ciphertext);expect(decryptSecret(a,key,"one")).toBe("sk-test");});it("authenticates context and every envelope field",()=>{const key=randomBytes(32),value=encryptSecret("sk-test",key,"one");expect(()=>decryptSecret(value,key,"two")).toThrow();expect(()=>decryptSecret({...value,ciphertext:value.ciphertext.slice(0,-2)+"AA"},key,"one")).toThrow();expect(()=>decryptSecret({...value,iv:value.iv.slice(0,-2)+"AA"},key,"one")).toThrow();expect(()=>decryptSecret({...value,authTag:value.authTag.slice(0,-2)+"AA"},key,"one")).toThrow();});});
